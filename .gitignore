# Node (gateway)
gateway/node_modules/
gateway/auth/*
!gateway/auth/.gitkeep

# .NET (brain)
brain/bin/
brain/obj/

# Runtime data (brain)
brain/logs/
brain/data/

# Misc
*.log
npm-debug.log*
.env

# Secrets & config operasional (jangan commit)
brain/config/secrets.json
brain/config/config.json
gateway/config.json

# Engine binary besar (~109M stockfish)
brain/engine/

# Backups & artefak recovery (besar / bukan source-of-truth)
backups/
RECOVERY/
*.bak
*.bak-mojibake
*.bak-prejudit
*.bak-prechess
*.bak-premod-*
brain/config/*.bak-*
gateway/*.bak-*

# Auth archive / backup (sesi WhatsApp - SENSITIF, jangan commit)
gateway/auth-archive-*/
gateway/auth-backup-*/
gateway/.last-auth-backup
gateway/.baileys-version-before

# Backup source decompile / eksperimen (bukan source-of-truth)
brain/*.bak-*
gateway-test7/

# WhatsApp QR (sensitif)
gateway/qr.png
gateway/qr.html

# Editor / OS
.vs/
Thumbs.db
brain/Properties/
*.bak-*
brain/*.bak-*
